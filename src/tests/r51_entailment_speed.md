# R51 ratio-only certificate entailment speed fix

The third AR epoch spent hours in `ZhangR51PhysicalStore::entails`, rebuilding a physical affine domain for every gauge certificate. The gauge scan does not mutate its physical archive or current chart until all candidate families have been collected.

The scan owns one immutable copy of H and b. Before dense HNF, a sparse row-variable adjacency map eliminates only unit pivots (coefficient ±1), updating affected rows and recording exact affine substitutions for future targets. Non-unit pivots and their divisibility conditions remain in the residual exact domain. It then computes row HNF and tests integer feasibility once. For a feasible domain, each transformed target is reduced against the HNF using exact rational coefficients; both the coefficient residual and right-hand-side residual must vanish. Neither a particular integer solution nor the saturated physical kernel is constructed. Unknown nonzero physical variables fail entailment. A direct-row receipt is accepted only after the history has been proved integer-feasible; this intentionally rejects direct receipts from an inconsistent history. No process/global cache is retained: new epochs, arcs, right-hand sides and call contexts create new snapshots. No integer search, ratio threshold, NIS policy, transport rule or precision tolerance is changed.

`ZHANG_R51_GAUGE_ENTAILMENT_CACHE` reports queries, direct queries, `affine_builds=0`, consequence builds, total build time, input nonzeros, unit pivots, fill added, residual rows/columns/nonzeros, HNF nonzeros, and sparse/HNF/integer-feasibility times. The earlier WL/L1 phase timers separate ILS, physical compatibility, joint HNF, root NIS and covariance conditioning. The maxima in `R48_EXACT_TIMING` are independent maxima, not a jointly observed matrix shape.

Validation: `zhang_r51_entailment_tests` now uses an independent rational Gaussian-elimination oracle for systems made feasible by a planted integer solution. Contradictory duplicates and nonprimitive infeasible equations are separate known-infeasible fixtures. The old reference copied the production algorithm and reused its core exact functions; it was unsuitable as the only independent oracle for this rewrite. The 64-variable/96-query benchmark is local-only and is not a whole-network speed forecast. A frozen real epoch and full AR rerun remain required before deployment.

Other AR costs remain: full KF factorization is unchanged, and each product image still needs exact HNF, integer feasibility, a particular integer solution and target-image HNF. NIS behavior follows the separate ratio-only switch. The first epoch's formerly uninstrumented region requires actual phase timers before attributing its cost.

## Product-domain ownership

`ZhangR47ProductSearchFrame` owns an immutable shared affine domain. A target frame created by `zhangR49CompileTargetOnDomain()` copies only the handle, not its particular solution, kernel, and HNF basis. Independently compiled WL/L1 domains remain separate objects; there is no cross-epoch or content-hash cache. `r51_performance_reference.cpp` checks handle identity for one domain and non-identity for different histories.

## Direct physical target projection

Only `IMAGE_GENERATORS_ONLY` now requests an exact particular solution without a physical kernel. The kernel's unimodular column operations are applied directly to the compact product target matrix, retaining its free columns `T*K` in the original kernel-column order. The full-kernel `INTEGER_PROJECTOR` path is unchanged and remains available as a differential reference. For 1,200 deterministic random domains, tests compare the projected free columns with full `T*K`, as well as `particularTarget`, `imageGenerators`, `projector` and `offsets` produced by both physical-image modes. Explicit nonprimitive and hidden-variable parity fixtures prevent accidental saturation of `2Z` or an odd coset. These are local algebraic checks, not proof of a whole-network speedup or product quality. On one 200-target/402-variable/50-history-row synthetic benchmark, domain compilation was 0.431 s versus 0.114 s; real-epoch profiling is still required.
