# R48 staged implementation
Base: R47 c4726888d81aade37a09e38e7268d37b83fc6b0e.
Acceptance thresholds and total 1e-3 family risk ceiling remain unchanged.
C0: terminal NIS diagnostics and small-domain snapshots; reject significant negative eigenvalues in NIS, gauge validation and exact KF conditioning. Lifecycle instrumentation follows with C3.
C1: batch all quotient unit right-hand sides in one exact decomposition, retaining LK=I; time official route, product image and history selection. Scalar/batch exact-combination regression covers quotient ranks 1 through 17.\n
C2: pure propose/audit/commit boundary. Healthy old trees have exact identity transport (unchanged existing paths); no heuristic score can replace them. Arc/QC/representation failures force rebuild and invalidate inherited component authorization. Nonidentity tree transport is deliberately unproven until recertified; this control does not copy old integer potentials.

C3: the source has a concrete observationEdges-only candidate gate although the controller retains same-arc stateEdges through configured grace. Separate stochastic support from direct AR candidates. Only existing finite propagated chord states in the current graph can support product combinations; direct WL/L1 still uses the original observation gate. No state is inserted, no variance is shrunk, no slip/QC gate is relaxed. KIRI/G27 raw/QC, cleanup, remove request, graph, full KF and search extraction are logged. R47 historical deletion versus filtering remains unproven until the instrumented run reaches that epoch.

C4: the official FLOAT/HISTORY routes now use a common-round-prior safe prefix. Each union passes exact feasibility, new rank and root joint NIS before becoming safe. A failed later block retains the prefix. At most one rescue per round (dimension <=2 block sizes) replays from the common prior without current-round answers; replacing the prefix drops its dependent decisions. Every attempt, including rejected/abandoned attempts, is charged. Legacy R47 helper remains unchanged for regression. Tests exercise late NIS failure, conflicting overlap and common-prior rescue.

C5: diagnose certified component cuts and verify alternate anchor equivalence on the same integer system. FLOAT and HISTORY each reserve 25% of their existing budget for at most four full 1/2-D bridge attempts. Search exact integer-image coordinates, pull accepted rows back to network coordinates, and recheck full affine feasibility/root joint NIS before rebuilding consequences. Missing support returns UNREPRESENTABLE; structural zeros do not publish certificates. All attempts count in final family proof. Prior pair names only affect deterministic ordering; old integers are never copied.

C6: exact feasibility/kernel/search work modes; directly lift only small product-image coordinates through the saturated kernel, avoiding the complete network left inverse. Batch image-generator memberships. Candidate FLOAT/HISTORY/bridge moments use low-rank projected conditioning with a cache scoped to one immutable root posterior; conditioner rows AND affine values are keys. No cross-epoch/branch cache. Final delivery retains full KF covariance checks. This removes candidate dense posterior copies; remaining complete KF delivery/preprocessing cost must be measured, not claimed real-time.

C6 diagnostics: exact feasibility/kernel/inverse/product-image operation times, dimensions, nonzero counts and coefficient bit lengths; transformed search means/covariances, column order, integer projectors/offsets and conditioner RHS at full precision. Extend the existing deterministic checkpoint interval whitelist from 60s to 30s/60s; controller roundtrip rejects mismatched intervals. Experiment captures each completed epoch for KF/graph/Ledger/proof/frontend/input-cursor replay. Checkpoint I/O is distinct from solver time.

C2 integration audit: preserve qualified old-tree edges before soft receiver-core proposal pruning as well as before tree-score optimization. Current physical/QC/quality gates still apply to every retained edge; enlarging a proposed core cannot reduce its existing satellite support.
